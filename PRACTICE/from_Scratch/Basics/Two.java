import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

// here we are removing the duplicates from the Array

class test{
    public static void main(String[] args) {
        List<String > enames=Arrays.asList("Siva","krishna","rahul","priyanka","priya","priya");
    //  enames.stream().distinct().collect(Collectors.toList());
    //   System.out.println(enames);

    // List<Integer> ids=Arrays.asList(101,102,103,104,104,105,105,106,106);
    
    //  for(int i=0;i=ids>;i++){
        
    //  }
    }

}
